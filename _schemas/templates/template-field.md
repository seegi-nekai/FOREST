---
type: field
name: <field-name>
status: active
parent: <container-name или пусто>
created: YYYY-MM-DD
updated: YYYY-MM-DD
git: false
---

# <Название поля>

<Одно-два предложения о том, что покрывает это поле.>

## Сюда относится

- <Тип материалов, которые живут в этом поле.>

## Сюда НЕ относится

- <Тип материалов, которые лежат в других полях. Указатели куда именно.>

## Особые правила для модели

<!-- Опциональный раздел. Только если в поле есть конвенции, отличающиеся от глобальных правил. Если правил нет — удалить раздел целиком. -->

<!--
Правила:
- Наличие `FIELD.md` — то, что делает директорию полем. В контейнере (родительской папке, группирующей поля) `FIELD.md` не создаётся.
- `name:` во frontmatter совпадает с именем директории.
- `parent:` — имя контейнера, если поле лежит внутри. Пусто для полей в корне vault.
- `status: active` для рабочих полей; `archived` — для закрытых.
- `git: true` — если поле выезжает в свой репозиторий и может подключаться к другим vault'ам.
- Внутренняя структура поля: `observations/`, `evergreens/`, `reflections/`.
-->
