---
type: observation
title: "<заголовок источника>"
source: "<URL или библиографическая ссылка>"
author: "<автор или [[Wikipedia]] и т. п.>"
created: YYYY-MM-DD
published: YYYY-MM-DD
tags:
  - <tag>
---

<Сырой текст источника. Кладётся как есть, без правок и редактуры. Это `sources` у Карпатого — неизменяемый слой.>

<!--
Правила:
- `type: observation` обязателен — это маркер слоя.
- `title` и `source` — обязательны: один из них URL, другой человекочитаемое название. Для книги/статьи в `source` идёт библиографическая запись.
- `author` — либо имя, либо wikilink на страницу автора, либо «[[Wikipedia]]» и т. п. для коллективных источников.
- `created` — когда положили в vault.
- `published` — дата публикации источника, если применимо. Если неизвестно — оставить пустым.
- Тело файла не редактируется после захвата. Если источник пришёл из Web Clipper и frontmatter отличается — модель приводит его к этой схеме при ingest, само тело не трогается.
- Заголовок H1 внутри файла не используется; имя файла = заголовок.
-->
