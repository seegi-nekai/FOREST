filters:
  and:
    - type == "task"
    - field == "forest-framework"
views:
  - type: cards
    name: Install Workflow
    groupBy:
      property: status
      direction: ASC
    order:
      - file.name
      - created
